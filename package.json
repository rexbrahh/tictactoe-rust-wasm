{
  "name": "tictactoe-wasm",
  "version": "1.0.0",
  "description": "Tic Tac Toe game with Rust/WASM AI",
  "scripts": {
    "build-wasm": "wasm-pack build --target web --out-dir pkg",
    "serve": "python3 -m http.server 8000",
    "dev": "npm run build-wasm && npm run serve"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
} 